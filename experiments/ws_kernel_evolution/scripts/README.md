# Scripts

Place reproducible entrypoints here.

Expected script categories:

- benchmark sweep scripts
- timeline / plot generation scripts
- correctness cross-check scripts
- environment dump helpers

When possible, scripts should write machine-readable outputs to `../data/`
instead of only printing to stdout.
