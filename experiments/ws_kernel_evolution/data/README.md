# Data Files

Put raw measurement outputs here.

Recommended contents per dataset:

- benchmark configuration
- exact code path under test
- median latency / TFLOPS
- all per-repetition samples
- environment metadata
- optional notes on validity

Use `manifest.csv` to keep a small human-readable index.
